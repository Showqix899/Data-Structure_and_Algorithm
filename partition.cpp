#include<bits/stdc++.h>
using namespace std;
int main(){
    int arr[]={3,4,2,6,7,8,11,5};
    int len=sizeof(arr)/sizeof(arr[0]);
    int pivot=arr[len-1];
    for(int i=0;i<len;i++){
        if(pivot<arr[i]){
            arr[]
        }
    }
}